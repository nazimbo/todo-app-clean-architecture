export declare class Task {
    id: string;
    title: string;
    description: string;
    dueDate: string;
    completed: boolean;
    constructor(id: string, title: string, description: string, dueDate: string, completed: boolean);
}
